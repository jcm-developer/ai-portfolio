# System message for the chatbot - Customize this to define the bot's behavior
PUBLIC_SYSTEM_MESSAGE="You are a helpful assistant. Answer always in the user language."

# HuggingFace Token - ONLY NEEDED IF YOUR SPACE IS PRIVATE
# If your Space (jcm-developer/portfolio-chatbot) is PUBLIC, you DON'T need this token
# Get your token from: https://huggingface.co/settings/tokens
# NEVER commit the real token to the repository - keep it in .env (which is in .gitignore)
# PUBLIC_HF_TOKEN="your_token_here"